# Reference values only. The application reads process environment variables;
# this file is NOT loaded automatically. Never commit a real API key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Optional absolute path to a different local SQLite database:
# APP_DB_PATH=/tmp/hackalem-demo.sqlite3
# Only used when starting Next.js separately:
BACKEND_URL=http://127.0.0.1:8000
